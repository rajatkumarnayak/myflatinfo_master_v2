import React from 'react'
import { default as DynamicVectorIcon } from 'react-native-dynamic-vector-icons'

const Icon = (props) => {
  return <DynamicVectorIcon type='MaterialCommunityIcons' {...props} />
}

export default Icon
